/**
 * This package contains the application ports.
 * The application defines these ports to communicate with the outside world. The port package is placed inside the application package, indicating that the application owns these ports.
 * <p>
 * 運用了依賴反轉原則(DIP)的方式。
 * 應用程式定義了這些轉接埠來與外部世界溝通，把 port 套件放在 application 套件之內，表示應用程式擁有這些轉接埠。
 */
package com.martin.cleanarchitecturedemo.application.port;